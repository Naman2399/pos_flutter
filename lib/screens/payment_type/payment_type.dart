import 'package:flutter/material.dart';

class PaymentTypePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Payment Type Page"),
      ),
    );
  }
}
