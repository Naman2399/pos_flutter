import 'package:flutter/material.dart';

class PromotionAndActionPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Promotion and Action Page"),
      ),
    );
  }
}
